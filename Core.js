import React from 'react';
import {SafeAreaView} from 'react-native';
import {Icon, Typography} from './components';

const Core = () => {
  return (
    <SafeAreaView>
      <Icon />
      <Typography />
    </SafeAreaView>
  );
};

export default Core;
